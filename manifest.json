{
  "manifest_version": 2,
 
  "name": "Skips Google Paid Ad Host Block",
  "version": "0.0.1",
  "description": "This extension helps environments where Google ad services host is blocked by DNS resulting in navigation errors when a paid result is clicked. It does this by disabling the URL masquerading that happens when the link is clicked.",
 
  "content_scripts": [{
    "js": ["content.js"],
    "matches": ["https://www.google.com.br/search*", "https://www.google.com/search*"]
  }]
 
}
